spring:
  datasource:
    url: jdbc:mysql://localhost:3306/FitCh?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: fitch
    password: fitch1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
jwt:
  secret: vocal-app-secret-key-must-be-at-least-32-characters-long-for-hs256
  access-token-expiration: 7200000
  refresh-token-expiration: 604800000
ai:
  mock: true
  server:
    url: http://localhost:8001
server:
  port: 8080

